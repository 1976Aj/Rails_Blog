
<%= render @article.comments %>

<%# Looping through all of the article comments %>
<p>
  <strong>Title:</strong>
  <%= @article.title %>
</p>

<p>
  <strong>Text:</strong>
  <%= @article.text %>
</p>


<h2 class="addComment">Add a comment:</h2>
<%= render 'comments/form' %>

<%= link_to 'Edit', edit_article_path(@article), class: "btn btn-success halfBtn" %>
<%= link_to 'Back', articles_path, class: "btn btn-success halfBtn" %>
