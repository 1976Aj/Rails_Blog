<%= render 'header', title: "Comments", text: "LIfe as a Front End Developer." %>
<p>
  <strong>Commenter:</strong>
  <%= comment.commenter %>
</p>

<p>
  <strong>Comment:</strong>
  <%= comment.body %>
</p>

<p>
  <%= link_to 'Delete Comment', [comment.article, comment],
               method: :delete,
               data: { confirm: 'Are you sure?' }, class:"btn btn-warning" %>
</p>

<%# Time of submission %>
<p><%= comment.created_at.localtime.strftime('on %A %b %d \'%y at %l:%M %p') %></p>
